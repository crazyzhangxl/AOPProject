<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aopproject">

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".login_demo.LoginActivity"
            android:label="登陆界面" />
        <activity
            android:name=".login_demo.UserActivity"
            android:label="用户界面" />
        <activity android:name=".arouter.ARouterMainActivity" />
        <activity android:name=".arouter.testactivity.Test2Activity" />
        <activity android:name=".arouter.testactivity.TestWebViewActivity" />
        <activity android:name=".arouter.testactivity.Test1Activity" />
        <activity android:name=".arouter.testactivity.Test4Activity"></activity>
    </application>

</manifest>